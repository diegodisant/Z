#include <stdio.h>
#include "../compiler/lexer/next-token.h"
#include "../compiler/parser/ParserDriver.h"

int main(){
	//call the driver and start the parser evaluation
	ParserDriver();
	return 0x0;
}